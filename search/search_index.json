{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Homelab","text":"<p>This is end-to-end documentation for building, rebuilding, and maintaining my homelab. It covers the full stack from bare hardware to running services.</p> <p>Some values (IP addresses, domain names, credentials references) are specific to my setup, but the procedures are generic enough to follow on any similar cluster.</p>"},{"location":"#stack-overview","title":"Stack overview","text":"Layer Technology Hardware 3\u00d7 Raspberry Pi 5 (16 GB) nodes Cluster k3s (lightweight Kubernetes) Virtual IP kube-vip Ingress Traefik TLS cert-manager + Cloudflare DNS-01 SSO Authentik Storage Longhorn Observability Loki + Prometheus + Grafana (LGTM) DNS Pi-hole Home Automation Home Assistant + Matter Server + OTBR Media Jellyfin"},{"location":"#nodes","title":"Nodes","text":"<p>Three identical Raspberry Pi 5 boards form the cluster. All are control-plane + etcd members (k3s embedded HA), so any one can be lost without taking the cluster down.</p> Hostname IP Spec Role <code>rpi01</code> <code>192.168.1.11</code> Raspberry Pi 5 Model B (16 GB RAM, 4\u00d7 Cortex-A76 @ arm64) control-plane, etcd, master <code>rpi02</code> <code>192.168.1.12</code> Raspberry Pi 5 Model B (16 GB RAM, 4\u00d7 Cortex-A76 @ arm64) control-plane, etcd, master <code>rpi03</code> <code>192.168.1.13</code> Raspberry Pi 5 Model B (16 GB RAM, 4\u00d7 Cortex-A76 @ arm64) control-plane, etcd, master <p>Common to all nodes: Debian 13 (Trixie), kernel <code>6.12.x-rpt-rpi-2712</code>, k3s <code>v1.33.6+k3s1</code>. Each board also has the SoC's onboard Broadcom Bluetooth (<code>hci0</code>, over the SoC UART) \u2014 used by the Matter Server on <code>rpi01</code> for BLE commissioning (see Home Assistant \u2192 Matter Server).</p>"},{"location":"#network-layout","title":"Network layout","text":"Address Role <code>192.168.1.1</code> Gateway <code>192.168.1.10</code> k3s VIP (kube-vip) <code>192.168.1.11</code> <code>rpi01</code> \u2014 control-plane / master <code>192.168.1.12</code> <code>rpi02</code> \u2014 control-plane <code>192.168.1.13</code> <code>rpi03</code> \u2014 control-plane <code>192.168.1.21</code> In-cluster Pi-hole (kube-vip) \u2014 set up but not currently in use <code>192.168.1.60</code> DNS server \u2014 sole resolver in use, including local servers <code>192.168.1.168</code> smhub (Nano MG24) \u2014 Thread Border Router (OpenThread REST :8081) <p>Internal services are exposed under <code>*.local.spaelling.xyz</code>, backed by Cloudflare-issued TLS certificates via DNS-01 challenge.</p>"},{"location":"#build-order","title":"Build order","text":"<p>Follow sections in this order for a clean build from scratch:</p> <ol> <li>Terminal \u2014 set up local tooling (Warp, Starship, Homebrew)</li> <li>k3s \u2014 prepare Raspberry Pi nodes, install k3s, kube-vip, Longhorn</li> <li>HTTPS \u2014 install cert-manager, then Traefik</li> <li>Authentication \u2014 install Authentik and configure SSO</li> <li>DNS \u2014 deploy Pi-hole</li> <li>Observability \u2014 deploy the LGTM stack</li> <li>Home Automation \u2014 deploy Home Assistant and add-ons</li> <li>Media \u2014 deploy Jellyfin</li> </ol>"},{"location":"authentik/authentik/","title":"Authentik","text":"<p>Authentik is the identity provider for the homelab. It provides SSO via OAuth2/OIDC and forward-auth, protecting internal services behind a login screen without each service needing its own authentication.</p> <p>Services integrated with Authentik: Grafana, Traefik dashboard, and any other internal web UI.</p> <p>Status: not currently deployed. Authentik is documented here but is not running in the cluster at the moment.</p>"},{"location":"authentik/authentik/#prerequisites","title":"Prerequisites","text":"<p>Traefik and cert-manager must be running before installing Authentik, as the Helm values reference an <code>IngressRoute</code> and a cert-manager <code>Certificate</code>.</p> <p>The <code>authentik-credentials.yaml</code> file contains the secret key and PostgreSQL password. This file is stored in the password vault and is not committed to the repository.</p>"},{"location":"authentik/authentik/#install","title":"Install","text":"<pre><code>kubectl create namespace authentik\nkubectl apply -f authentik-certificate.yaml\nhelm repo add authentik https://charts.goauthentik.io\nhelm repo update\nhelm upgrade --install authentik authentik/authentik --namespace=authentik -f authentik-values.yaml,authentik-credentials.yaml\n</code></pre> <p><code>authentik-values.yaml</code> is in this directory. <code>authentik-credentials.yaml</code> comes from the password vault.</p>"},{"location":"authentik/authentik/#verify-pods-are-healthy","title":"Verify pods are healthy","text":"<p>Check the PostgreSQL database first \u2014 Authentik will not start if the database is unavailable:</p> <pre><code>kubectl describe pods -n authentik -l app.kubernetes.io/name=postgresql\n</code></pre> <p>Then check the Authentik server and worker:</p> <pre><code>kubectl describe pods -n authentik -l app.kubernetes.io/name=authentik,app.kubernetes.io/component=server\nkubectl describe pods -n authentik -l app.kubernetes.io/name=authentik,app.kubernetes.io/component=worker\n</code></pre> <p>And Redis (used for session caching):</p> <pre><code>kubectl describe pods -n authentik -l app.kubernetes.io/name=redis\n</code></pre> <p>Check the TLS certificate, secret, and ingress:</p> <pre><code>kubectl describe certificates authentik-tls -n authentik\nkubectl get secret authentik-tls -n authentik -o yaml\nkubectl get ingress -n authentik -o yaml\n</code></pre> <p>Test the endpoint:</p> <pre><code>curl -kv https://authentik.local.spaelling.xyz/\n</code></pre> <p>If the response shows <code>subject: CN=TRAEFIK DEFAULT CERT</code>, the Authentik TLS secret is not being picked up by Traefik. Check the <code>IngressRoute</code> to confirm it references the correct secret name, and verify the certificate is in <code>Ready</code> state.</p>"},{"location":"authentik/authentik/#first-login","title":"First login","text":"<p>Navigate to the initial setup flow:</p> <pre><code>https://authentik.local.spaelling.xyz/if/flow/initial-setup/\n</code></pre> <ol> <li>Create the first admin user</li> <li>Add the user to the <code>authentik Admins</code> group</li> <li>Go to Settings \u2192 MFA Devices and register a passkey</li> <li>Deactivate the default <code>akadmin</code> user</li> </ol>"},{"location":"authentik/authentik/#certificates","title":"Certificates","text":"<p>Two certificates are needed:</p> <ol> <li>Service certificate \u2014 for the Authentik web UI itself (<code>authentik-certificate.yaml</code>)</li> <li>Outpost certificate \u2014 for the embedded outpost that handles forward-auth for other services</li> </ol> <p>The outpost certificate is referenced in <code>authentik-values.yaml</code> and is created by the same cert-manager <code>ClusterIssuer</code>.</p>"},{"location":"authentik/authentik/#logs","title":"Logs","text":"<pre><code>kubectl logs -n authentik -l app.kubernetes.io/name=authentik,app.kubernetes.io/component=server --tail=-1 &gt; authentik-server.log\n\nkubectl logs -n authentik -l app.kubernetes.io/name=postgresql --tail=-1 &gt; authentik-postgresql.log\n</code></pre>"},{"location":"authentik/authentik/#restart","title":"Restart","text":"<p>If the server is in a bad state, a rollout restart is usually sufficient:</p> <pre><code>kubectl rollout restart deployment authentik-server -n authentik\n</code></pre>"},{"location":"authentik/authentik/#uninstall","title":"Uninstall","text":"<p>Helm uninstall does not remove Persistent Volume Claims. Delete them manually to fully clean up:</p> <pre><code>helm uninstall authentik --namespace=authentik\n\nkubectl get pvc -n authentik -o jsonpath=\"{.items[*].metadata.name}\" | tr ' ' '\\n' | xargs -I {} kubectl delete pvc {} -n authentik\n\nkubectl delete secret authentik-secret-key --namespace authentik\nkubectl delete secret authentik-postgresql-password --namespace authentik\n</code></pre>"},{"location":"dns/pihole/nebula-sync/","title":"Nebula Sync","text":"<p>Nebula Sync keeps multiple Pi-hole instances in sync. It replicates configuration (blocklists, whitelists, DNS records, settings) from a primary Pi-hole to one or more replicas, ensuring consistent behaviour across instances.</p>"},{"location":"dns/pihole/nebula-sync/#why","title":"Why","text":"<p>Running multiple Pi-hole replicas (as a StatefulSet) without synchronisation means each instance has an independent configuration. Any change made through the web UI of one instance is not reflected in the others. Nebula Sync solves this by treating one instance as the source of truth and pushing changes to the rest.</p>"},{"location":"dns/pihole/nebula-sync/#setup","title":"Setup","text":"<p>Note</p> <p>Nebula Sync integration is not yet deployed. The following describes the intended configuration.</p> <p>Deploy Nebula Sync as a Kubernetes <code>CronJob</code> in the <code>pihole</code> namespace. It should run frequently (e.g. every 5 minutes) to keep replicas in sync.</p> <p>The CronJob needs:</p> <ul> <li>The Pi-hole API URL and password for the primary instance</li> <li>The Pi-hole API URL and password for each replica</li> </ul> <p>Secrets for the Pi-hole admin password already exist as <code>pihole-webpassword</code> in the <code>pihole</code> namespace.</p>"},{"location":"dns/pihole/nebula-sync/#references","title":"References","text":"<ul> <li>Nebula Sync GitHub</li> <li>Pi-hole v6 API</li> </ul>"},{"location":"dns/pihole/pihole/","title":"Pi-hole","text":"<p>Pi-hole provides DNS with network-wide ad blocking for the homelab. This page documents the in-cluster Pi-hole, which runs in k3s with a dedicated IP (<code>192.168.1.21</code>) assigned by kube-vip.</p> <p>Not currently in use. This <code>192.168.1.21</code> instance is set up but is not active in the network today, and it does not hold records for local network servers (so it cannot resolve the <code>*.local.spaelling.xyz</code> internal domain). The sole DNS server in use is the primary resolver at <code>192.168.1.60</code>. The plan is to grow this into a redundant set of in-cluster Pi-hole resolvers \u2014 with local records \u2014 and eventually serve DNS from the cluster.</p> <p>The deployment uses the pihole-kubernetes classic manifests.</p>"},{"location":"dns/pihole/pihole/#install","title":"Install","text":"<p>Apply the kube-vip ConfigMap so Pi-hole gets its dedicated IP, then create the namespace and deploy:</p> <pre><code>kubectl apply -f kubevip-configmap.yaml\nkubectl create namespace pihole\nkubectl create secret -n pihole generic pihole-webpassword --from-literal=\"password=$(openssl rand -base64 64)\"\nkubectl apply -f pihole.yaml\n</code></pre> <p><code>kubevip-configmap.yaml</code> is in the <code>k3s/kubevip</code> directory. The generated password is stored as a Kubernetes secret \u2014 retrieve it later with <code>kubectl get secret -n pihole pihole-webpassword -o jsonpath='{.data.password}' | base64 --decode</code>.</p>"},{"location":"dns/pihole/pihole/#verify-dns-is-working","title":"Verify DNS is working","text":"<p>Once pods are running, test that Pi-hole is answering DNS queries on its dedicated IP:</p> <pre><code>tcping -f 4 -t 5 192.168.1.21 53\nnslookup google.com 192.168.1.21\n</code></pre> <p>Both should succeed. If <code>nslookup</code> returns an answer from <code>192.168.1.21</code>, Pi-hole is operational.</p>"},{"location":"dns/pihole/pihole/#expose-the-admin-ui","title":"Expose the admin UI","text":"<p>Apply the ingress for the primary Pi-hole instance:</p> <pre><code>kubectl apply -f pihole-0-ingress.yaml\n</code></pre> <p>This creates a Traefik <code>IngressRoute</code> for the Pi-hole web UI.</p>"},{"location":"dns/pihole/pihole/#check-logs","title":"Check logs","text":"<pre><code># Latest logs from the primary instance\nkubectl logs pihole-0 -n pihole\n\n# Stream logs in real time\nkubectl logs -f pihole-0 -n pihole\n\n# Logs from all replicas simultaneously\nkubectl logs -l app=pihole -n pihole --all-containers\n\n# DNS query log (inside the pod)\nkubectl exec -it pihole-0 -n pihole -- tail -f /var/log/pihole/pihole.log\n</code></pre>"},{"location":"dns/pihole/pihole/#check-rollout-status","title":"Check rollout status","text":"<p>After making changes to the StatefulSet (e.g. updating the image or config), monitor the rollout:</p> <pre><code>kubectl rollout status statefulset pihole -n pihole\n</code></pre>"},{"location":"home_assistant/home_assistant/","title":"Home Assistant","text":"<p>Home Assistant is the home automation platform. It runs in the <code>home-assistant</code> namespace alongside the Matter Server and a VS Code Server for config editing. (The Thread Border Router runs externally on the SMHUB Nano MG24, not in the cluster.)</p>"},{"location":"home_assistant/home_assistant/#deploy","title":"Deploy","text":"<pre><code>kubectl create namespace home-assistant\nkubectl apply -f home_assistant.yaml\n</code></pre>"},{"location":"home_assistant/home_assistant/#update-after-configmap-changes","title":"Update after ConfigMap changes","text":"<p>Home Assistant does not watch its ConfigMap for changes. After editing any ConfigMap, trigger a rollout:</p> <pre><code>kubectl rollout restart deployment home-assistant -n home-assistant\nkubectl rollout status deployment home-assistant -n home-assistant\n</code></pre> <p>If the rollout hangs (old pods not terminating), force it by scaling to zero and back:</p> <pre><code>kubectl -n home-assistant scale deploy/home-assistant --replicas=0\nkubectl -n home-assistant get pods\nkubectl -n home-assistant delete rs -l app=home-assistant\nkubectl -n home-assistant scale deploy/home-assistant --replicas=1\nkubectl -n home-assistant rollout status deploy/home-assistant\n</code></pre>"},{"location":"home_assistant/home_assistant/#troubleshooting","title":"Troubleshooting","text":"<p>Inspect certificate issuance \u2014 Home Assistant uses a cert-manager certificate like all other services:</p> <pre><code>kubectl get certificates -n home-assistant --no-headers -o custom-columns=\":metadata.name\" | xargs -I {} kubectl describe certificates {} -n home-assistant\n\nkubectl get certificaterequests -n home-assistant --no-headers -o custom-columns=\":metadata.name\" | xargs -I {} kubectl describe certificaterequests {} -n home-assistant\n\nkubectl get order -n home-assistant --no-headers -o custom-columns=\":metadata.name\" | xargs -I {} kubectl describe order {} -n home-assistant\n\nkubectl get challenges -n home-assistant --no-headers -o custom-columns=\":metadata.name\" | xargs -I {} kubectl describe challenges {} -n home-assistant\n</code></pre> <p>Open a shell inside the pod:</p> <pre><code>kubectl exec -n home-assistant -it deploy/home-assistant -- sh\n</code></pre>"},{"location":"home_assistant/home_assistant/#hacs","title":"HACS","text":"<p>HACS (Home Assistant Community Store) must be installed via an <code>initContainer</code> because the Home Assistant container is ephemeral \u2014 anything written to the container filesystem outside of <code>/config</code> is lost on restart. The <code>initContainer</code> copies HACS into the persistent volume at <code>/config/custom_components/hacs/</code> before the main container starts.</p>"},{"location":"home_assistant/home_assistant/#verify-hacs-is-present","title":"Verify HACS is present","text":"<pre><code>kubectl exec -it &lt;pod-name&gt; -n home-assistant -- ls -l /config/custom_components/hacs/manifest.json\n</code></pre>"},{"location":"home_assistant/home_assistant/#verify-home-assistant-detected-hacs","title":"Verify Home Assistant detected HACS","text":"<pre><code>kubectl logs -l app=home-assistant -n home-assistant --tail=100 | grep \"hacs\"\n</code></pre>"},{"location":"home_assistant/home_assistant/#clear-the-component-cache-if-hacs-is-present-but-missing-from-the-ui","title":"Clear the component cache (if HACS is present but missing from the UI)","text":"<pre><code>kubectl exec -it -n home-assistant \"$(kubectl get pod -n home-assistant -l app=home-assistant -o jsonpath='{.items[0].metadata.name}')\" -- rm -rf /config/.storage/custom_components\n</code></pre> <p>Restart the pod after clearing the cache. Enable Advanced Mode in your Home Assistant User Profile before searching for HACS in the integrations list.</p> <p>If the search bar is unresponsive, force the setup flow directly:</p> <pre><code>https://home-assistant.local.spaelling.xyz/config/integrations/dashboard/add?domain=hacs\n</code></pre>"},{"location":"home_assistant/home_assistant/#vs-code-server","title":"VS Code Server","text":"<p>The VS Code Server pod provides a browser-based IDE for editing Home Assistant configuration files directly in the <code>/config</code> persistent volume.</p> <p>Create the password secret:</p> <pre><code>kubectl create secret generic vscode-password --namespace=home-assistant --from-literal=code-server-password='your_actual_password_here'\n</code></pre> <p>Deploy:</p> <pre><code>kubectl apply -f code-server.yaml\n</code></pre> <p>The editor is available at <code>https://code.home-assistant.local.spaelling.xyz</code>.</p>"},{"location":"home_assistant/home_assistant/#matter-server","title":"Matter Server","text":"<p>The Matter Server enables Home Assistant to communicate with Matter-compatible smart home devices.</p> <pre><code>kubectl apply -f matter_server.yaml\n</code></pre> <p>Persistence \u2014 <code>--storage-path /data</code> is required. The container args override the image's default command, so <code>--storage-path /data</code> must be passed explicitly. Without it the server writes its controller database (<code>chip.json</code>, holding the fabric and all commissioned nodes) to <code>/root/.matter_server/</code> on the ephemeral container filesystem instead of the PVC, and loses every onboarded device on each restart. Confirm it is persisting to the volume:</p> <pre><code>kubectl exec -n home-assistant deploy/matter-server -- ls -l /data/chip.json\n</code></pre> <p>Service connectivity. Home Assistant connects to the Matter Server over a websocket. The <code>matter-server-service</code> (ClusterIP) selects pods by <code>app: matter-server</code> \u2014 keep <code>externalTrafficPolicy</code> out of the <code>selector</code> block or the Service ends up with no endpoints. The Matter integration in HA can then use <code>ws://matter-server-service.home-assistant:5580/ws</code>; the deployment is also pinned to <code>rpi01</code> via nodeAffinity, so the node IP (<code>ws://192.168.1.11:5580/ws</code>) is a stable fallback.</p> <p>Check the logs to confirm it started successfully:</p> <pre><code>kubectl logs -f deployment/matter-server -n home-assistant\n</code></pre> <p>Look for these lines:</p> <pre><code>INFO [matter_server.server.server] Starting the Matter Server...\nINFO [matter_server.server.helpers.paa_certificates] Fetching the latest PAA root certificates...\nINFO [matter_server.server.server] Matter Server successfully initialized.\n</code></pre> <p>If the rollout gets stuck, apply the same scale-to-zero approach used for Home Assistant:</p> <pre><code>kubectl -n home-assistant scale deploy/matter-server --replicas=0\nkubectl -n home-assistant delete rs -l app=matter-server\nkubectl -n home-assistant scale deploy/matter-server --replicas=1\nkubectl -n home-assistant rollout status deploy/matter-server\n</code></pre>"},{"location":"home_assistant/home_assistant/#verify-matter-server-connectivity","title":"Verify Matter Server connectivity","text":"<p>Get the node IP the pod is running on and test the Matter port:</p> <pre><code>HOST_IP=\"$(kubectl get pod -n home-assistant -l app=matter-server -o jsonpath='{.items[0].status.hostIP}')\"\necho \"Matter Server is on node $HOST_IP\"\ntcping -f 4 -t 5 \"$HOST_IP\" 5580\n</code></pre> <p>Using the retrieved <code>hostIP</code> keeps the test correct even when the pod schedules on a different node.</p>"},{"location":"home_assistant/home_assistant/#commissioning-matter-over-thread-devices","title":"Commissioning Matter-over-Thread devices","text":"<p>This is the gotcha for this homelab. Most current IKEA devices (e.g. TIMMERFLOTTE temperature/humidity sensor) are Matter-over-Thread, not Zigbee. Commissioning a factory-reset Matter-over-Thread device needs two things at pairing time:</p> <ol> <li>A Bluetooth (BLE) handshake with the device while it advertises in    pairing mode, and</li> <li>handing the device the Thread operational dataset so it can join the    mesh.</li> </ol> <p>The phone path is a dead end here. With a self-built OTBR (the smhub Nano MG24) and an iPhone, iOS refuses Thread commissioning because Apple only accepts an Apple-ecosystem border router (HomePod/Apple TV); the \"Thread Border Router required\" popup comes from iOS itself, before the request ever reaches Home Assistant. Android would work, but is not available here.</p> <p>Phone-free commissioning via the cluster's own Bluetooth. The Raspberry Pi nodes have an onboard Bluetooth controller, and <code>bluetoothd</code> runs on the host. By mounting the host D-Bus system bus into the Matter Server pod and passing <code>--bluetooth-adapter 0</code>, the in-cluster Matter Server does the BLE handshake itself \u2014 no phone in the loop. See the <code>--bluetooth-adapter</code> flag and the <code>/run/dbus</code> <code>hostPath</code> mount in <code>matter_server.yaml</code>. The CHIP SDK's native BLE stack talks to BlueZ on <code>rpi01</code> (where the deployment is pinned via nodeAffinity).</p> <p>Workflow:</p> <ol> <li>Confirm the smhub border router and a usable dataset exist in HA:    Settings \u2192 Devices &amp; Services \u2192 Thread \u2014 the smhub should be listed,    marked preferred, with credentials.</li> <li>Make sure the Matter Server has BLE enabled (above) and is running on    <code>rpi01</code>.</li> <li>Put the device into pairing mode (e.g. factory-reset an IKEA TIMMERFLOTTE so    its LED blinks rapidly) and place it physically near <code>rpi01</code> \u2014 the onboard    Pi Bluetooth is low-power, so keep it within a few metres / same room.</li> <li>Commission the device programmatically. The HA UI's Add Matter device flow    insists on the phone companion app, but driving the Matter Server directly    works headless: use the commissioning scripts (<code>commission-thread-devices.sh</code>    or <code>Invoke-MatterCommission.ps1</code>) or the WebSocket API below, passing the    11-digit pairing code (e.g. <code>3062-912-8403</code>). The Matter Server scans over    BLE, performs PASE, hands over the Thread dataset, and the device joins the    smhub mesh.</li> </ol> <p>Range and timing are the usual failure. A <code>commission_with_code</code> that ends in <code>BLEManagerImpl.cpp: CHIP Error 0x32: Timeout</code> followed by <code>Discovery timed out</code> means the BLE adapter worked but no device advertisement was heard in the ~20 s scan window \u2014 i.e. the device was not in pairing mode, its window had closed, or it was out of range of <code>rpi01</code>. Re-trigger pairing mode, move the device next to <code>rpi01</code>, and retry. (The parallel <code>Wi-Fi PAF</code> / <code>Long discriminator is required</code> error in the same log is an unrelated discovery transport and can be ignored.)</p> <p>Future: HA's network-Bluetooth-proxy commissioning (server-side groundwork in HA 2026.06 / Matter Server 8.5) will let any HA Bluetooth proxy on the LAN provide the BLE leg, relaxing the \"near <code>rpi01</code>\" constraint.</p>"},{"location":"home_assistant/home_assistant/#manual-commissioning-via-the-matter-server-websocket-api","title":"Manual commissioning via the Matter Server WebSocket API","text":"<p>When the HA UI flow is flaky, you can drive the Matter Server directly over its WebSocket API (<code>ws://&lt;matter-server&gt;:5580/ws</code>) \u2014 this is exactly what HA does under the hood, and it gives you the raw CHIP error if something fails. For a Thread device you must set the Thread dataset on the controller first, then commission. The command names are the API command strings (e.g. <code>set_thread_dataset</code>, <code>commission_with_code</code>), not the Python method names.</p> <pre><code># 1. Grab the active Thread dataset TLV (hex) HA holds for the smhub network:\nkubectl exec -n home-assistant deploy/home-assistant -c home-assistant -- python3 -c 'import json;d=json.load(open(\"/config/.storage/thread.datasets\"))[\"data\"]; print(next(x[\"tlv\"] for x in d[\"datasets\"] if x[\"id\"]==d[\"preferred_dataset\"]))'\n\n# 2. From inside the matter-server pod, set the dataset then commission the code.\n#    network_only=false lets it commission a brand-new (BLE) device.\nkubectl exec -i -n home-assistant deploy/matter-server -- env TLV=\"&lt;tlv-hex&gt;\" python3 - &lt;&lt;'PY'\nimport asyncio, json, os, aiohttp\nasync def main():\n    async with aiohttp.ClientSession() as s, s.ws_connect(\"ws://localhost:5580/ws\", heartbeat=30) as ws:\n        await ws.receive_str()                       # server_info banner\n        async def cmd(mid, command, **args):\n            await ws.send_str(json.dumps({\"message_id\": mid, \"command\": command, \"args\": args}))\n            while True:\n                m = json.loads(await ws.receive_str())\n                if m.get(\"message_id\") == mid:\n                    return m\n        print(\"set_thread_dataset:\", await cmd(\"d\", \"set_thread_dataset\", dataset=os.environ[\"TLV\"]))\n        print(\"commission:\", await cmd(\"c\", \"commission_with_code\",\n                                       code=\"3062-912-8403\", network_only=False))\nasyncio.run(main())\nPY\n</code></pre> <p>A successful run returns a node id (e.g. <code>node_id: 2, available: True</code>); HA then auto-creates the entities (<code>sensor.&lt;device&gt;_temperature</code>, <code>\u2026_humidity</code>, <code>\u2026_battery</code>, etc.). Because the Matter Server persists to the PVC (<code>--storage-path /data</code>), the node survives pod restarts. Confirm a live read with <code>get_node</code>, or inspect nodes with the <code>get_nodes</code> command.</p>"},{"location":"home_assistant/home_assistant/#bluetooth-ownership-matter-server-only-not-home-assistant","title":"Bluetooth ownership \u2014 Matter Server only, not Home Assistant","text":"<p><code>rpi01</code> has exactly one Bluetooth controller (<code>hci0</code>), and both Home Assistant and the Matter Server are pinned to <code>rpi01</code>. The Matter Server owns that adapter for commissioning, so Home Assistant's own Bluetooth integration must not also try to claim it \u2014 an HA Bluetooth proxy here would be pointless (same node, same radio) and the two would contend.</p> <p>Home Assistant has no D-Bus mount, so its auto-discovered Bluetooth adapter entries can never connect and spew <code>habluetooth.scanner \u2026 'NoneType' object has no attribute 'send'</code> every ~30 s. Disable those entries: in Settings \u2192 Devices &amp; Services \u2192 Bluetooth, disable each <code>Raspberry Pi (Trading) Ltd \u2026</code> adapter (they will not be re-enabled by discovery once disabled). The errors stop immediately and Matter is unaffected.</p>"},{"location":"home_assistant/home_assistant/#otbr-openthread-border-router","title":"OTBR (OpenThread Border Router)","text":"<p>OTBR bridges Thread devices (e.g. Matter over Thread) to the IP network.</p> <p>The Thread Border Router runs on the SMHUB Nano MG24 at <code>192.168.1.168</code>, exposing the OpenThread REST API on port 8081. Home Assistant's OpenThread Border Router integration points at <code>http://192.168.1.168:8081</code>.</p> <p>Verify the border router is healthy (should return JSON with <code>\"State\": \"leader\"</code> and the network name):</p> <pre><code>curl -s http://192.168.1.168:8081/node\ncurl -s http://192.168.1.168:8081/node/dataset/active\n</code></pre> <p>In Home Assistant, Settings \u2192 Devices &amp; Services \u2192 Thread should list this border router and show a preferred dataset whose border-agent ID and extended address match the <code>BaId</code> / <code>ExtAddress</code> returned by <code>/node</code>.</p>"},{"location":"home_assistant/home_assistant/#energy-management-nordpool-heatpump-automation","title":"Energy management \u2014 Nordpool &amp; Heatpump automation","text":""},{"location":"home_assistant/home_assistant/#install-hacs-integrations","title":"Install HACS integrations","text":"<p>After HACS is set up, install via the HACS UI:</p> <ul> <li>Nordpool \u2014 electricity spot price sensor</li> <li>Energi Data Service \u2014 Danish grid tariff data</li> <li>ApexCharts Card \u2014 custom Lovelace chart card</li> </ul>"},{"location":"home_assistant/home_assistant/#heatpump-automation","title":"Heatpump automation","text":"<p>The heatpump control logic adjusts the target temperature based on electricity spot prices. It uses three Helpers defined in Settings \u2192 Devices &amp; Services \u2192 Helpers:</p> Type Name Entity ID Purpose Dropdown Heatpump Strategy <code>input_select.heatpump_strategy</code> Current mode (Boost / Normal / Reduce / Off) Number Heatpump Base Temperature <code>input_number.heatpump_base_temperature</code> Standard target temperature Number Heatpump Boost Value <code>input_number.heatpump_boost_value</code> Delta applied in Boost/Reduce mode <p>The target temperature is calculated as <code>Base \u00b1 Delta</code> depending on the selected strategy.</p> <p>Place the Python script in the persistent volume using VS Code Server:</p> <pre><code>https://code.home-assistant.local.spaelling.xyz/?folder=/config\n</code></pre> <p>Copy <code>heatpump_control.py</code> to <code>/config/python_scripts/</code>.</p> <p>Apply the script and automation manifests (create as automation in the UI or via YAML):</p> <pre><code>heatpump_control.yaml       # the script\nheatpump_control_steering.yaml  # the automation that triggers it\n</code></pre> <p>Create the secret with Warmlink API credentials for the heatpump:</p> <pre><code>kubectl create secret generic heatpump-warmlink-credentials --from-literal=HEATPUMP_USER='PLACEHOLDER' --from-literal=HEATPUMP_PASS='PLACEHOLDER' --from-literal=HEATPUMP_DEVICE_CODE='PLACEHOLDER' -n home-assistant\n</code></pre> <p>Verify the credentials are loaded as environment variables in the Home Assistant pod:</p> <pre><code>kubectl exec -it $(kubectl get pods -n home-assistant -l app=home-assistant -o jsonpath='{.items[0].metadata.name}') -n home-assistant -- env | grep HEATPUMP_\n</code></pre>"},{"location":"home_assistant/home_assistant/#utility-scripts","title":"Utility scripts","text":"<p>Helper scripts for factory-resetting IKEA bulbs by power-cycling the smart plug they are connected to. Each takes a <code>switch</code> entity as input and toggles it on a fixed schedule \u2014 the bulb resets after the required number of off/on cycles.</p> <pre><code>reset_ikea_bulb.yaml       # 6 power cycles (standard TRADFRI bulb)\nreset_ikea_kajplats.yaml   # 12 power cycles (KAJPLATS bulb)\n</code></pre> <p>These live in Home Assistant as scripts (Settings \u2192 Automations &amp; Scenes \u2192 Scripts). Paste the YAML into a new script in YAML-edit mode, or add the contents under the matching key in <code>/config/scripts.yaml</code>.</p>"},{"location":"k3s/housekeeping/","title":"Housekeeping","text":"<p>Basic housekeeping tasks to perform on each node before installing k3s. These apply after the Raspberry Pi prerequisites are complete.</p>"},{"location":"k3s/housekeeping/#change-passwords","title":"Change passwords","text":"<p>The default <code>pi</code> user password must be changed before the node is connected to the network. Also change the root password:</p> <pre><code>passwd\npasswd root\n</code></pre>"},{"location":"k3s/housekeeping/#system-update","title":"System update","text":"<p>Bring the system fully up to date before installing anything:</p> <pre><code>sudo apt update &amp;&amp; sudo apt upgrade -y\n</code></pre> <p>Reboot after the upgrade if the kernel was updated:</p> <pre><code>sudo reboot\n</code></pre>"},{"location":"k3s/housekeeping/#install-useful-tools","title":"Install useful tools","text":"<p>A few tools are useful throughout the setup process:</p> <pre><code>sudo apt install -y curl jq nano\n</code></pre> <ul> <li><code>curl</code> \u2014 used for downloading manifests and testing endpoints</li> <li><code>jq</code> \u2014 used to parse JSON output (e.g. when fetching kube-vip release versions)</li> <li><code>nano</code> \u2014 for editing config files on the node</li> </ul>"},{"location":"k3s/housekeeping/#set-the-hostname","title":"Set the hostname","text":"<p>Give each node a meaningful hostname that matches its role. This makes it easier to identify nodes in <code>kubectl get nodes</code> output:</p> <pre><code>sudo hostnamectl set-hostname rpi-1\n</code></pre> <p>Repeat on each node with an appropriate name (<code>rpi-1</code>, <code>rpi-2</code>, etc.).</p> <p>Update <code>/etc/hosts</code> on each node to include all other nodes:</p> <pre><code>sudo nano /etc/hosts\n</code></pre> <p>Add entries such as:</p> <pre><code>192.168.1.11  rpi-1\n192.168.1.12  rpi-2\n</code></pre>"},{"location":"k3s/housekeeping/#verify-time-sync","title":"Verify time sync","text":"<p>k3s is sensitive to clock drift between nodes. Confirm NTP is running:</p> <pre><code>timedatectl status\n</code></pre> <p><code>System clock synchronized: yes</code> and <code>NTP service: active</code> should both be present.</p>"},{"location":"k3s/k3sup/","title":"k3sup","text":"<p>k3sup is a CLI tool that installs k3s on remote nodes over SSH and writes the kubeconfig to your local machine. It removes the need to SSH in manually and run the k3s install script by hand.</p>"},{"location":"k3s/k3sup/#install-k3sup","title":"Install k3sup","text":"<pre><code>brew install k3sup\n</code></pre> <p>Note</p> <p>Most commands in the following sections run on the remote node with elevated privileges. k3sup handles this via <code>--sudo true</code>. If you need to run anything manually on the node, either use <code>sudo</code> or switch to root with <code>su</code>.</p>"},{"location":"k3s/k3sup/#install-the-master-node","title":"Install the master node","text":"<p>If a kubeconfig from a previous cluster exists, remove it first to avoid conflicts:</p> <pre><code>rm -f ~/.kube/config\n</code></pre> <p>Install k3s on the first node. Key flags explained:</p> <ul> <li><code>--cluster</code> \u2014 enables embedded etcd for HA (required even for a single server when you plan to add more)</li> <li><code>--no-extras</code> \u2014 disables the bundled Traefik and ServiceLB (we install our own)</li> <li><code>--tls-san</code> \u2014 adds extra SANs to the API server certificate so the VIP hostname and IP are both valid</li> <li><code>--merge</code> \u2014 merges into an existing kubeconfig rather than overwriting</li> </ul> <pre><code>k3sup install --user pi --sudo true --ip 192.168.1.11 --cluster --k3s-channel stable --no-extras --local-path ~/.kube/config --merge --tls-san 192.168.1.10 --tls-san k3s.local.spaelling.xyz\n</code></pre>"},{"location":"k3s/k3sup/#validate-the-installation","title":"Validate the installation","text":"<p>Point kubectl at the new config and check the node is ready:</p> <pre><code>export KUBECONFIG=~/.kube/config\nkubectl config use-context default\nkubectl get node -o wide\n</code></pre> <p>Check the k3s systemd service on the node:</p> <pre><code>systemctl status k3s.service\ncat /etc/systemd/system/k3s.service\n</code></pre> <p>Look for these flags in the <code>ExecStart</code> line:</p> <pre><code>--tls-san k3s.local.spaelling.xyz --tls-san 192.168.1.10 --disable servicelb --disable traefik\n</code></pre> <p>If any of these are missing, the install did not apply all options correctly. Add the missing flags to the service file and restart:</p> <pre><code>systemctl daemon-reload\nsystemctl restart k3s.service\n</code></pre>"},{"location":"k3s/k3sup/#join-additional-nodes","title":"Join additional nodes","text":"<p>To add a second server node (not an agent \u2014 this node also runs the control plane):</p> <pre><code>k3sup join --user pi --ip 192.168.1.12 --sudo true --server --server-ip 192.168.1.11 --server-user pi --k3s-channel stable --no-extras\n</code></pre> <p><code>--server</code> makes this a server node rather than a worker-only agent. Omit it if you want a pure worker node.</p> <p>After joining, verify the cluster sees all nodes:</p> <pre><code>kubectl get nodes -o wide\n</code></pre>"},{"location":"k3s/k3sup/#uninstall-k3s","title":"Uninstall k3s","text":"<p>Run the uninstall script on each node individually. This removes k3s, its data, and the systemd service:</p> <pre><code>sudo /usr/local/bin/k3s-uninstall.sh\n</code></pre> <p>On agent-only nodes, the script is named <code>k3s-agent-uninstall.sh</code>:</p> <pre><code>sudo /usr/local/bin/k3s-agent-uninstall.sh\n</code></pre>"},{"location":"k3s/kubevip/","title":"kube-vip","text":"<p>kube-vip provides a virtual IP (VIP) for the k3s API server and a load balancer for Kubernetes services. It uses ARP-based leader election to move the VIP between nodes if the current leader goes down.</p> <p>In this setup:</p> <ul> <li>VIP: <code>192.168.1.10</code> \u2014 DNS name <code>k3s.local.spaelling.xyz</code></li> <li>Interface: <code>eth0</code></li> </ul> <p>kube-vip is deployed as a DaemonSet on control-plane nodes, managed by k3s's auto-deploy manifests directory (<code>/var/lib/rancher/k3s/server/manifests/</code>).</p>"},{"location":"k3s/kubevip/#install-kube-vip","title":"Install kube-vip","text":"<p>All commands run on the master node as root.</p>"},{"location":"k3s/kubevip/#create-the-manifests-directory","title":"Create the manifests directory","text":"<p>k3s watches this directory and automatically deploys any YAML files placed in it:</p> <pre><code>mkdir -p /var/lib/rancher/k3s/server/manifests/\n</code></pre>"},{"location":"k3s/kubevip/#upload-the-rbac-manifest","title":"Upload the RBAC manifest","text":"<p>kube-vip needs cluster-level RBAC permissions to watch nodes and services:</p> <pre><code>curl https://kube-vip.io/manifests/rbac.yaml &gt; /var/lib/rancher/k3s/server/manifests/kube-vip-rbac.yaml\n</code></pre>"},{"location":"k3s/kubevip/#generate-the-daemonset-manifest","title":"Generate the DaemonSet manifest","text":"<p>Fetch the latest kube-vip release version and generate the manifest. The <code>alias</code> wraps <code>ctr</code> (containerd) to run kube-vip as a one-shot container to produce the YAML:</p> <pre><code>export VIP=192.168.1.10\nexport INTERFACE=eth0\napt install -y jq curl\nKVVERSION=$(curl -sL https://api.github.com/repos/kube-vip/kube-vip/releases | jq -r \".[0].name\")\nalias kube-vip=\"ctr image pull ghcr.io/kube-vip/kube-vip:$KVVERSION; ctr run --rm --net-host ghcr.io/kube-vip/kube-vip:$KVVERSION vip /kube-vip\"\n\nkube-vip manifest daemonset --interface $INTERFACE --address $VIP --inCluster --taint --controlplane --services --arp --leaderElection | tee /var/lib/rancher/k3s/server/manifests/kube-vip.yaml\n</code></pre> <p>Key flags:</p> <ul> <li><code>--taint</code> \u2014 only runs on control-plane nodes</li> <li><code>--controlplane</code> \u2014 manages the API server VIP</li> <li><code>--services</code> \u2014 also manages LoadBalancer service IPs</li> <li><code>--arp</code> \u2014 uses ARP for IP advertisement (no BGP required)</li> <li><code>--leaderElection</code> \u2014 enables leader election between control-plane nodes</li> </ul> <p>k3s picks up the new manifest automatically. Verify the pods are running:</p> <pre><code>kubectl get pods -n kube-system -l app.kubernetes.io/name=kube-vip\n</code></pre>"},{"location":"k3s/kubevip/#configure-dns","title":"Configure DNS","text":"<p>Add an A record pointing <code>k3s.local.spaelling.xyz</code> to <code>192.168.1.10</code> in your DNS server (Pi-hole or your router).</p> <p>Update the kubeconfig to use the DNS name instead of the node IP, so the config remains valid even if nodes change:</p> <pre><code>sudo nano ~/.kube/config\n</code></pre> <p>Change the <code>server:</code> line to:</p> <pre><code>server: https://k3s.local.spaelling.xyz:6443\n</code></pre>"},{"location":"k3s/kubevip/#install-the-kube-vip-cloud-provider","title":"Install the kube-vip Cloud Provider","text":"<p>The cloud provider controller handles <code>LoadBalancer</code> service IP assignment. It reads from a ConfigMap to determine which IP pool to use:</p> <pre><code>kubectl apply -f https://raw.githubusercontent.com/kube-vip/kube-vip-cloud-provider/main/manifest/kube-vip-cloud-controller.yaml\n</code></pre> <p>Note</p> <p>The ConfigMap that tells the cloud provider which IP range to assign is created later, during Traefik setup. Traefik is the only service that needs a LoadBalancer IP, and it gets <code>192.168.1.10</code>.</p>"},{"location":"k3s/longhorn/","title":"Longhorn","text":"<p>Longhorn is a distributed block storage system for Kubernetes. It provides persistent volumes backed by replicated storage across nodes, with a web UI for managing volumes, snapshots, and backups.</p> <p>Longhorn replaces the default local-path storage class that k3s ships with, giving persistent volumes that survive node failure.</p>"},{"location":"k3s/longhorn/#check-prerequisites","title":"Check prerequisites","text":"<p>Longhorn has specific node requirements. The <code>longhornctl</code> tool checks them automatically.</p> <p>First, make the k3s kubeconfig accessible:</p> <pre><code>mkdir -p ~/.kube\nsudo install -o \"$USER\" -g \"$USER\" -m 600 /etc/rancher/k3s/k3s.yaml ~/.kube/config\n</code></pre> <p>Using <code>install</code> copies the file with your user as the owner, so later <code>kubectl</code> calls can read it. Avoid <code>sudo cp</code>, which leaves <code>~/.kube/config</code> owned by root and unreadable to your normal user.</p> <p>Download the <code>longhornctl</code> binary for ARM64 (Raspberry Pi):</p> <pre><code>curl -sSfL -o longhornctl https://github.com/longhorn/cli/releases/download/v1.9.0/longhornctl-linux-arm64\nsudo chmod +x longhornctl\n</code></pre> <p>Run the preflight check:</p> <pre><code>sudo KUBECONFIG=/etc/rancher/k3s/k3s.yaml ./longhornctl check preflight\n</code></pre> <p>If the check reports missing packages (e.g. <code>open-iscsi</code>, <code>nfs-common</code>), the installer can handle them automatically across all nodes:</p> <pre><code>sudo ./longhornctl --kube-config ~/.kube/config --image longhornio/longhorn-cli:v1.9.0 install preflight\n</code></pre>"},{"location":"k3s/longhorn/#install-with-helm","title":"Install with Helm","text":"<p>Install Helm if not already available:</p> <pre><code>brew install helm\n</code></pre> <p>Add the Longhorn Helm chart repository and install into a dedicated namespace:</p> <pre><code>helm repo add longhorn https://charts.longhorn.io\nhelm repo update\nhelm install longhorn longhorn/longhorn --namespace longhorn-system --create-namespace --version 1.9.0\n</code></pre> <p>Wait for all pods to come up \u2014 Longhorn deploys several components (manager, driver, UI):</p> <pre><code>kubectl -n longhorn-system get pod --watch\n</code></pre> <p>All pods should reach <code>Running</code> state. This can take a few minutes on the first install.</p>"},{"location":"k3s/longhorn/#expose-the-ui","title":"Expose the UI","text":"<p>Longhorn's frontend service is initially behind a LoadBalancer. We delete that and replace it with a ClusterIP so Traefik can manage ingress:</p> <pre><code>kubectl delete service longhorn-frontend -n longhorn-system\nkubectl apply -f longhorn-frontend.yaml\nkubectl apply -f certificate.yaml\nkubectl apply -f ingress.yaml\n</code></pre> <p>Wait for cert-manager to issue the certificate before testing the UI:</p> <pre><code>kubectl describe certificate longhorn-web-ui-tls -n longhorn-system\n</code></pre> <p><code>Status: True</code> and <code>Reason: Ready</code> in the conditions means TLS is working. The UI will be available at <code>https://longhorn.local.spaelling.xyz</code>.</p> <p>Note</p> <p>The ingress and certificate manifests (<code>longhorn-frontend.yaml</code>, <code>certificate.yaml</code>, <code>ingress.yaml</code>) live alongside this documentation. Traefik and cert-manager must be installed and working before applying them.</p>"},{"location":"k3s/sources/","title":"Sources","text":"<p>Reference links for the k3s section.</p> <ul> <li>k3sup \u2014 CLI installer for k3s</li> <li>k3s documentation \u2014 official k3s docs</li> <li>kube-vip \u2014 virtual IP and load balancer</li> <li>kube-vip k3s guide \u2014 k3s-specific kube-vip setup</li> <li>kube-vip Cloud Provider \u2014 LoadBalancer IP assignment</li> <li>Longhorn \u2014 distributed block storage</li> <li>Longhorn installation requirements</li> </ul>"},{"location":"k3s/ssh/","title":"SSH","text":"<p>SSH key-based authentication is required for k3sup to install and manage k3s nodes without interactive password prompts.</p>"},{"location":"k3s/ssh/#generate-an-ssh-key-pair","title":"Generate an SSH key pair","text":"<p>If you do not already have a key pair, generate one. The <code>-C</code> flag adds a comment to identify the key:</p> <pre><code>ssh-keygen -t rsa -b 4096 -C \"homelab\"\n</code></pre> <p>This creates <code>~/.ssh/id_rsa</code> (private key) and <code>~/.ssh/id_rsa.pub</code> (public key). Keep the private key secure and never share it.</p>"},{"location":"k3s/ssh/#copy-the-public-key-to-each-node","title":"Copy the public key to each node","text":"<p>Run this for every node that k3sup will manage. Replace the IP with each node's static address:</p> <pre><code>ssh-copy-id -i ~/.ssh/id_rsa.pub pi@192.168.1.11\nssh-copy-id -i ~/.ssh/id_rsa.pub pi@192.168.1.12\n</code></pre> <p>This appends your public key to <code>~/.ssh/authorized_keys</code> on the remote host, allowing passwordless login.</p>"},{"location":"k3s/ssh/#verify-the-connection","title":"Verify the connection","text":"<p>Test that key-based auth works before proceeding to the k3s installation:</p> <pre><code>ssh -i ~/.ssh/id_rsa pi@192.168.1.11\n</code></pre> <p>If the connection succeeds without prompting for a password, the node is ready for k3sup.</p>"},{"location":"k3s/ssh/#passwordless-sudo","title":"Passwordless sudo","text":"<p>k3sup runs commands on the remote nodes with <code>--sudo true</code>, which requires passwordless sudo for the <code>pi</code> user. Verify this is configured on each node:</p> <pre><code>sudo visudo\n</code></pre> <p>The <code>pi</code> user (or the <code>sudo</code> group) should have a line like:</p> <pre><code>%sudo   ALL=(ALL:ALL) NOPASSWD: ALL\n</code></pre>"},{"location":"k3s/prerequisites/rpi.prereqs/","title":"Raspberry Pi Prerequisites","text":"<p>These steps prepare a fresh Raspberry Pi for k3s. They must be completed on each node before running the k3s installer.</p> <p>All commands run as root on the Pi unless noted otherwise. Connect via serial console or a temporary DHCP address initially.</p>"},{"location":"k3s/prerequisites/rpi.prereqs/#enable-cgroups","title":"Enable cgroups","text":"<p>k3s requires cgroup support for CPU and memory isolation. On Raspberry Pi OS, cgroups are not enabled by default and must be added to the kernel command line.</p> <p>Edit the boot command line:</p> <pre><code>nano /boot/firmware/cmdline.txt\n</code></pre> <p>Append the following to the end of the single line \u2014 do not add a newline:</p> <pre><code>cgroup_enable=cpuset cgroup_memory=1 cgroup_enable=memory\n</code></pre> <p>Reboot, then verify the parameters were applied:</p> <pre><code>cat /proc/cmdline\n</code></pre> <p>You should see <code>cgroup_enable=cpuset cgroup_memory=1 cgroup_enable=memory</code> in the output.</p>"},{"location":"k3s/prerequisites/rpi.prereqs/#disable-swap","title":"Disable swap","text":"<p>k3s (and Kubernetes in general) requires swap to be disabled. Swap can cause unpredictable memory pressure behaviour in containerised workloads.</p> <p>Edit the swap configuration:</p> <pre><code>nano /etc/dphys-swapfile\n</code></pre> <p>Set <code>CONF_SWAPSIZE=0</code>, then apply and disable:</p> <pre><code>apt update\napt install dphys-swapfile -y\napt remove systemd-zram-generator -y\ndphys-swapfile swapoff\ndphys-swapfile setup\n</code></pre>"},{"location":"k3s/prerequisites/rpi.prereqs/#set-a-static-ip-address","title":"Set a static IP address","text":"<p>A static IP is required so that k3sup can reliably connect to each node and so the cluster has stable addresses.</p> <p>Edit <code>/etc/hosts</code> so the hostname resolves to the static IP. Keep the <code>127.0.0.1 localhost</code> line untouched \u2014 changing it breaks localhost resolution. Instead, point the <code>127.0.1.1 &lt;hostname&gt;</code> line at the static IP (or add a <code>&lt;static-ip&gt; &lt;hostname&gt;</code> line):</p> <pre><code>sudo nano /etc/hosts\n</code></pre> <p>For example, on node 1:</p> <pre><code>127.0.0.1       localhost\n192.168.1.11    node1\n</code></pre> <p>Verify the hostname resolves to the expected address:</p> <pre><code>hostname --ip-address\n</code></pre> <p>Configure the static IP using <code>nmcli</code>. Adjust <code>ipv4.addresses</code>, <code>ipv4.gateway</code>, and <code>ipv4.dns</code> to match your network:</p> <pre><code>nmcli connection add type ethernet ifname eth0 con-name static-eth0 ipv4.addresses 192.168.1.11/24 ipv4.gateway 192.168.1.1 ipv4.dns 192.168.1.60 ipv4.method manual\nnmcli connection up static-eth0\n</code></pre> <p>Confirm with:</p> <pre><code>ip a\n</code></pre>"},{"location":"k3s/prerequisites/rpi.prereqs/#configure-fan-control","title":"Configure fan control","text":"<p>The Raspberry Pi 5 active cooler fan can be noisy at low loads. Configure it to only spin when the CPU temperature exceeds a threshold.</p> <pre><code>sudo nano /boot/firmware/config.txt\n</code></pre> <p>Add the following at the end of the file. <code>gpiopin=14</code> is the default for the official Raspberry Pi active cooler. <code>temp=70000</code> means 70 \u00b0C (value is in millidegrees):</p> <pre><code>dtoverlay=gpio-fan,gpiopin=14,temp=70000\n</code></pre> <p>Reboot for the overlay to take effect.</p>"},{"location":"k3s/prerequisites/rpi.prereqs/#checking-temperature-and-fan-state","title":"Checking temperature and fan state","text":"<p>Monitor CPU temperature in real time:</p> <pre><code>watch -n 1 vcgencmd measure_temp\n</code></pre> <p>Check whether the fan is currently running:</p> <pre><code>pinctrl get 14\n</code></pre> <p><code>hi</code> means the fan is on (100% speed). <code>lo</code> means the fan is off.</p> <p>Alternatively, read the cooling device state directly (0 = off, 255 = full speed):</p> <pre><code>cat /sys/class/thermal/cooling_device0/cur_state\n</code></pre>"},{"location":"lgtm/grafana/grafana/","title":"Grafana","text":"<p>Grafana is the visualisation and dashboarding layer of the LGTM stack. It connects to Loki for logs and Prometheus for metrics, and is protected by Authentik SSO.</p>"},{"location":"lgtm/grafana/grafana/#prerequisites","title":"Prerequisites","text":"<ul> <li><code>lgtm</code> namespace exists</li> <li>cert-manager and Traefik are running</li> <li>Authentik is configured and the OAuth2 provider and application are set up for Grafana (see below)</li> </ul>"},{"location":"lgtm/grafana/grafana/#install","title":"Install","text":"<pre><code>helm repo add grafana https://grafana.github.io/helm-charts\nhelm repo update\nkubectl apply -f certificate.yaml\nkubectl apply -f grafana-oauth-secret.yaml\nhelm install grafana grafana/grafana --namespace lgtm -f grafana-values.yaml\n</code></pre> <p><code>grafana-oauth-secret.yaml</code> contains the Authentik OAuth2 client credentials and is not committed to the repository \u2014 retrieve it from the password vault.</p> <p>All available Helm values are in the Grafana Helm chart README.</p> <p>Verify the pod is running:</p> <pre><code>kubectl get pods -n lgtm -l app.kubernetes.io/name=grafana\n</code></pre> <p>Test the endpoint:</p> <pre><code>curl -kv https://grafana.local.spaelling.xyz/\n</code></pre>"},{"location":"lgtm/grafana/grafana/#initial-admin-password","title":"Initial admin password","text":"<p>The Helm chart generates a random admin password on first install. Retrieve it to make other users admin before SSO is working:</p> <pre><code>kubectl get secret --namespace lgtm grafana -o jsonpath=\"{.data.admin-password}\" | base64 --decode ; echo\n</code></pre> <p>Log in as <code>admin</code> with this password, promote your Authentik-linked user to admin, then you can rely on SSO going forward.</p>"},{"location":"lgtm/grafana/grafana/#upgrade","title":"Upgrade","text":"<pre><code>helm upgrade grafana grafana/grafana --namespace lgtm -f grafana-values.yaml\n</code></pre> <p>If pods get stuck after an upgrade (new pods in <code>Pending</code> or <code>Error</code> while old pods are still running), delete the stuck pods \u2014 Kubernetes will reschedule them:</p> <pre><code>kubectl delete pod -n lgtm -l app.kubernetes.io/name=grafana\n</code></pre>"},{"location":"lgtm/grafana/grafana/#authentik-integration","title":"Authentik integration","text":"<p>Follow the Authentik Grafana integration guide to create the OAuth2 provider and application in Authentik.</p> <p>The <code>grafana-values.yaml</code> in this directory already includes the <code>grafana.ini</code> settings for Authentik OAuth2. The only thing needed is the <code>grafana-oauth-secret.yaml</code> containing the client ID and secret generated by Authentik.</p>"},{"location":"lgtm/loki/loki/","title":"Loki","text":"<p>Loki is the log aggregation component of the LGTM stack. It stores and indexes logs from all pods in the cluster, queryable via Grafana.</p>"},{"location":"lgtm/loki/loki/#prerequisites","title":"Prerequisites","text":"<p>Create the <code>lgtm</code> namespace before installing any component in this stack \u2014 Loki, Prometheus, and Grafana all share it:</p> <pre><code>kubectl create namespace lgtm\n</code></pre>"},{"location":"lgtm/loki/loki/#install","title":"Install","text":"<pre><code>helm repo add grafana https://grafana.github.io/helm-charts\nhelm repo update\nhelm install loki grafana/loki -f loki-values.yaml -n lgtm\n</code></pre> <p>Verify the pods are running:</p> <pre><code>kubectl get pods -n lgtm -l app.kubernetes.io/name=loki\n</code></pre>"},{"location":"lgtm/loki/loki/#upgrade","title":"Upgrade","text":"<pre><code>helm upgrade loki grafana/loki --values loki-values.yaml -n lgtm\n</code></pre>"},{"location":"lgtm/loki/loki/#internal-cluster-endpoints","title":"Internal cluster endpoints","text":"<p>Other services in the cluster send logs and query Loki using its cluster-internal DNS name:</p> Use URL Push logs to Loki <code>http://loki-gateway.lgtm.svc.cluster.local/loki/api/v1/push</code> Grafana datasource <code>http://loki-gateway.lgtm.svc.cluster.local/</code>"},{"location":"lgtm/loki/loki/#kubernetes-log-monitoring","title":"Kubernetes log monitoring","text":"<p>The <code>k8s-monitoring</code> Helm chart deploys Grafana Alloy as a DaemonSet to collect logs and metrics from every node and forward them to Loki and Prometheus:</p> <pre><code>helm install k3smon grafana/k8s-monitoring --values k3s-monitoring-values.yml -n lgtm\n</code></pre> <p>To upgrade:</p> <pre><code>helm upgrade k3smon grafana/k8s-monitoring --values k3s-monitoring-values.yml -n lgtm\n</code></pre> <p>Validate that Alloy is collecting and forwarding logs:</p> <pre><code>kubectl logs -n lgtm -l app.kubernetes.io/name=alloy --tail=50\n</code></pre> <p>Look for lines confirming successful connections to the Loki push endpoint.</p> <p>All Alloy Helm values are documented in the Alloy chart repository.</p>"},{"location":"lgtm/prometheus/prometheus/","title":"Prometheus","text":"<p>Prometheus is the metrics backend of the LGTM stack. The <code>kube-prometheus-stack</code> Helm chart bundles Prometheus, Alertmanager, and a set of pre-built Kubernetes dashboards and alerting rules.</p>"},{"location":"lgtm/prometheus/prometheus/#prerequisites","title":"Prerequisites","text":"<p>The <code>lgtm</code> namespace must exist \u2014 create it if not already done (see Loki).</p>"},{"location":"lgtm/prometheus/prometheus/#install","title":"Install","text":"<pre><code>helm repo add prometheus-community https://prometheus-community.github.io/helm-charts\nhelm repo update\nhelm install prometheus prometheus-community/kube-prometheus-stack --values prometheus-values.yaml -n lgtm\n</code></pre> <p>Verify the stack is running:</p> <pre><code>kubectl --namespace lgtm get pods -l \"release=prometheus\"\n</code></pre> <p>The chart deploys several components: <code>prometheus-server</code>, <code>alertmanager</code>, <code>kube-state-metrics</code>, and <code>node-exporter</code> pods on each node.</p>"},{"location":"lgtm/prometheus/prometheus/#upgrade","title":"Upgrade","text":"<pre><code>helm upgrade prometheus prometheus-community/kube-prometheus-stack --values prometheus-values.yaml -n lgtm\n</code></pre>"},{"location":"lgtm/prometheus/prometheus/#grafana-datasource-url","title":"Grafana datasource URL","text":"<p>When configuring Prometheus as a datasource in Grafana, use the cluster-internal service URL:</p> <pre><code>http://prometheus-kube-prometheus-prometheus.lgtm.svc.cluster.local:9090\n</code></pre> <p>This is already set in <code>grafana-values.yaml</code> \u2014 no manual configuration is needed unless you are setting up Grafana fresh.</p>"},{"location":"lgtm/prometheus/prometheus/#reference","title":"Reference","text":"<ul> <li>kube-prometheus-stack chart</li> </ul>"},{"location":"servarr/servarr/","title":"Servarr","text":"<p>The <code>servarr</code> namespace hosts the media stack. Currently this consists of Jellyfin. The namespace is shared across all media services.</p>"},{"location":"servarr/servarr/#create-the-namespace","title":"Create the namespace","text":"<pre><code>kubectl create namespace servarr\n</code></pre> <p>This must be done before deploying any service in this section.</p>"},{"location":"servarr/servarr/#services","title":"Services","text":"Service Description Jellyfin Media server for streaming video and music"},{"location":"servarr/jellyfin/jellyfin/","title":"Jellyfin","text":"<p>Jellyfin is an open-source media server. It streams video, music, and photos to browsers and clients on the local network, protected by Traefik and Authentik.</p>"},{"location":"servarr/jellyfin/jellyfin/#prerequisites","title":"Prerequisites","text":"<p>The <code>servarr</code> namespace must exist \u2014 create it first if not already done (see Servarr).</p> <p>The deployment uses Kustomize overlays to manage environment-specific configuration.</p>"},{"location":"servarr/jellyfin/jellyfin/#deploy","title":"Deploy","text":"<pre><code>kubectl apply -k overlay\n</code></pre> <p>Run this from the <code>docs/servarr/jellyfin/</code> directory where the <code>overlay/</code> folder lives.</p> <p>Verify the pod comes up:</p> <pre><code>kubectl get pods -n servarr -l app=jellyfin\n</code></pre>"},{"location":"servarr/jellyfin/jellyfin/#first-run","title":"First run","text":"<p>On first start, Jellyfin runs a setup wizard. Navigate to <code>https://jellyfin.local.spaelling.xyz</code> and follow the prompts to:</p> <ol> <li>Create the initial admin user</li> <li>Add media libraries (point to the Longhorn PVC mount paths)</li> <li>Configure transcoding if hardware acceleration is available</li> </ol>"},{"location":"servarr/jellyfin/jellyfin/#upgrade","title":"Upgrade","text":"<p>To update Jellyfin, change the image tag in the Kustomize overlay and re-apply:</p> <pre><code>kubectl apply -k overlay\n</code></pre> <p>Jellyfin does not perform zero-downtime restarts \u2014 expect a brief interruption during the pod restart.</p>"},{"location":"terminal/starship/starship/","title":"Starship","text":"<p>Starship is a fast, cross-shell prompt written in Rust. It shows contextual information (git branch, k8s context, exit codes) without slowing down the shell.</p>"},{"location":"terminal/starship/starship/#installation","title":"Installation","text":"<pre><code>brew install starship\n</code></pre> <p>Then add the init hook to your shell config. For <code>zsh</code>, add to <code>~/.zshrc</code>:</p> <pre><code>eval \"$(starship init zsh)\"\n</code></pre>"},{"location":"terminal/starship/starship/#configuration","title":"Configuration","text":"<p>Starship reads from <code>~/.config/starship.toml</code>. Download the current config:</p> <pre><code>curl -o ~/.config/starship.toml https://raw.githubusercontent.com/spaelling/homelab-pages/main/docs/terminal/starship/starship.toml\n</code></pre> <p>Or copy it from this repo \u2014 the file is embedded below:</p> starship.toml<pre><code># Get editor completions based on the config schema\n\"$schema\" = 'https://starship.rs/config-schema.json'\n\n# Inserts a blank line between shell prompts\nadd_newline = true\n\n# Replace the '\u276f' symbol in the prompt with '\u279c'\n[character] # The name of the module we are configuring is 'character'\nsuccess_symbol = '[\u279c](bold green)' # The 'success_symbol' segment is being set to '\u279c' with the color 'bold green'\n\n# Disable the package module, hiding it from the prompt completely\n[package]\ndisabled = true\n\n[helm]\nformat = 'via [\u2388 $version](bold white) '\n</code></pre>"},{"location":"terminal/warp/warp/","title":"Warp","text":"<p>Warp is a modern, GPU-accelerated terminal emulator with built-in AI assistance, command history search, and block-based output. It is the primary terminal used for all homelab work.</p>"},{"location":"terminal/warp/warp/#installation","title":"Installation","text":"<p>Download and install from warp.dev. No Homebrew formula is needed \u2014 Warp ships as a native macOS app.</p>"},{"location":"terminal/warp/warp/#why-warp","title":"Why Warp","text":"<ul> <li>Block-based output makes it easy to copy individual command results without selecting around prompts</li> <li>Built-in command search and history filtering</li> <li>Works well with Starship for a rich prompt (see Starship)</li> </ul>"},{"location":"traefik/cert_manager/","title":"cert-manager","text":"<p>cert-manager automates the provisioning and renewal of TLS certificates. It integrates with Let's Encrypt using the DNS-01 challenge via Cloudflare, which means certificates can be issued for internal services without exposing them to the public internet.</p>"},{"location":"traefik/cert_manager/#install","title":"Install","text":"<p>Install Helm if not already available:</p> <pre><code>brew install helm\n</code></pre> <p>Add the Jetstack chart repository and install cert-manager with CRD support:</p> <pre><code>helm repo add jetstack https://charts.jetstack.io --force-update\nhelm install cert-manager jetstack/cert-manager --namespace cert-manager --create-namespace --version v1.18.0 --set crds.enabled=true\n</code></pre> <p>Verify all pods are running:</p> <pre><code>kubectl -n cert-manager get pod\n</code></pre>"},{"location":"traefik/cert_manager/#cloudflare-issuer-configuration","title":"Cloudflare issuer configuration","text":"<p>cert-manager uses a Cloudflare API token to prove domain ownership via DNS-01. The token is stored as a Kubernetes secret.</p>"},{"location":"traefik/cert_manager/#create-the-cloudflare-api-token","title":"Create the Cloudflare API token","text":"<ol> <li>Go to the Cloudflare API Tokens page</li> <li>Click Create Token and select the Edit zone DNS template</li> <li>Under Zone Resources, select the specific zone (<code>spaelling.xyz</code>)</li> <li>Optionally restrict the token to your cluster's egress IP under Client IP Address Filtering</li> <li>Create the token and save it \u2014 it is only shown once</li> </ol>"},{"location":"traefik/cert_manager/#apply-the-secret-and-issuer","title":"Apply the secret and issuer","text":"<p>The manifests are in <code>docs/traefik/</code>:</p> <pre><code>cd ~/git/Privat/homelab-pages/docs/traefik\nkubectl apply -n cert-manager -f cloudflare_api_token.yaml\nkubectl apply -f acme_clusterissuer.yaml\n</code></pre> <p><code>cloudflare_api_token.yaml</code> creates a secret containing the API token. <code>acme_clusterissuer.yaml</code> defines a <code>ClusterIssuer</code> that references that secret and configures the Let's Encrypt ACME server.</p> <p>For reference, see:</p> <ul> <li>cert-manager Cloudflare DNS-01 docs</li> <li>Creating a ClusterIssuer</li> </ul>"},{"location":"traefik/cert_manager/#force-recursive-dns-for-acme-challenges","title":"Force recursive DNS for ACME challenges","text":"<p>By default, cert-manager uses the cluster's internal DNS to verify challenges. This can cause failures when the cluster's DNS points to Pi-hole or another local resolver that does not resolve <code>*.acme.invalid</code>. Force cert-manager to use public DNS resolvers instead:</p> <pre><code>kubectl edit deployment cert-manager -n cert-manager\n</code></pre> <p>Find the <code>args:</code> list under <code>containers</code> and add the two <code>--dns01</code> flags:</p> <pre><code>containers:\n  - args:\n    - --v=2\n    - --cluster-resource-namespace=$(POD_NAMESPACE)\n    - --leader-election-namespace=kube-system\n    - --dns01-recursive-nameservers-only\n    - --dns01-recursive-nameservers=1.1.1.1:53,8.8.8.8:53\n</code></pre> <p>Restart cert-manager to apply the change:</p> <pre><code>kubectl rollout restart deployment cert-manager -n cert-manager\n</code></pre>"},{"location":"traefik/cert_manager/#troubleshooting","title":"Troubleshooting","text":"<p>Certificate issuance goes through several stages: <code>Certificate</code> \u2192 <code>CertificateRequest</code> \u2192 <code>Order</code> \u2192 <code>Challenge</code>. Inspect each in turn to find where a failure occurs:</p> <pre><code>kubectl get certificates -n traefik --no-headers -o custom-columns=\":metadata.name\" | xargs -I {} kubectl describe certificates {} -n traefik\n\nkubectl get certificaterequests -n traefik --no-headers -o custom-columns=\":metadata.name\" | xargs -I {} kubectl describe certificaterequests {} -n traefik\n\nkubectl get order -n traefik --no-headers -o custom-columns=\":metadata.name\" | xargs -I {} kubectl describe order {} -n traefik\n\nkubectl get challenges -n traefik --no-headers -o custom-columns=\":metadata.name\" | xargs -I {} kubectl describe challenges {} -n traefik\n</code></pre> <p>Common causes of failure:</p> <ul> <li>API token lacks DNS edit permissions for the zone</li> <li>Challenge still pending because Cloudflare DNS propagation is slow (wait a minute and retry)</li> <li>cert-manager using local DNS \u2014 apply the recursive DNS fix above</li> </ul>"},{"location":"traefik/traefik/","title":"Traefik","text":"<p>Traefik is the ingress controller for the cluster. It handles all inbound HTTP/HTTPS traffic, terminates TLS, and routes requests to services based on <code>IngressRoute</code> rules.</p> <p>k3s is installed with <code>--no-extras</code> which disables the bundled Traefik. We install it separately via Helm to have full control over the configuration.</p>"},{"location":"traefik/traefik/#install","title":"Install","text":"<p>Add the Traefik Helm chart repository:</p> <pre><code>helm repo add traefik https://traefik.github.io/charts\nhelm repo update\n</code></pre> <p>Create the namespace and install with custom values:</p> <pre><code>kubectl create ns traefik\nhelm install --namespace=traefik --values=./traefik-values.yml traefik traefik/traefik\n</code></pre> <p>All available Helm values are documented in the Traefik Helm chart.</p> <p>Verify the pod is running and has an external IP assigned by kube-vip:</p> <pre><code>kubectl get pods --namespace traefik\nkubectl get svc --namespace traefik\n</code></pre> <p>The <code>traefik</code> service should have an <code>EXTERNAL-IP</code> of <code>192.168.1.10</code>.</p>"},{"location":"traefik/traefik/#configure-the-kube-vip-cloud-provider","title":"Configure the kube-vip cloud provider","text":"<p>Create the ConfigMap that tells the kube-vip cloud provider which IP to assign to Traefik's LoadBalancer service. The manifest is in the <code>k3s/kubevip</code> directory:</p> <pre><code>kubectl apply -f kubevip-configmap.yaml\n</code></pre> <p>This maps the <code>traefik</code> namespace service to the VIP <code>192.168.1.10</code>.</p>"},{"location":"traefik/traefik/#create-the-traefik-dashboard-certificate","title":"Create the Traefik dashboard certificate","text":"<p>Issue a TLS certificate for the Traefik dashboard:</p> <pre><code>kubectl apply -f dashboard-certificate.yaml\n</code></pre>"},{"location":"traefik/traefik/#upgrade","title":"Upgrade","text":"<p>When changing values in <code>traefik-values.yml</code>, upgrade the release rather than reinstalling:</p> <pre><code>helm upgrade --namespace=traefik --values=./traefik-values.yml traefik traefik/traefik\n</code></pre>"},{"location":"traefik/traefik/#authentik-middleware","title":"Authentik middleware","text":"<p>Once Authentik is running, apply the forward-auth middleware to protect the Traefik dashboard and other services:</p> <pre><code>kubectl apply -f authentik-middleware.yaml\n</code></pre> <p>This creates a Traefik <code>Middleware</code> resource that forwards authentication decisions to Authentik's outpost.</p>"},{"location":"traefik/traefik/#troubleshooting","title":"Troubleshooting","text":"<p>Check pod status and describe them if not ready:</p> <pre><code>kubectl get pods -n traefik\nkubectl get pods -n traefik --no-headers -o custom-columns=\":metadata.name\" | xargs -I {} kubectl describe pod {} -n traefik\n</code></pre> <p>Check the IngressRoute and TLS secret for the dashboard:</p> <pre><code>kubectl describe ingressroute traefik-dashboard -n traefik\nkubectl describe secrets traefik-dashboard-tls -n traefik\n</code></pre> <p>Dump Traefik logs to a file for detailed inspection:</p> <pre><code>kubectl logs -n traefik -l app.kubernetes.io/name=traefik &gt; traefik.log\n</code></pre> <p>If a service shows the default Traefik certificate instead of a Let's Encrypt certificate, check that the <code>IngressRoute</code> references the correct TLS secret name and that the cert-manager certificate is in <code>Ready</code> state.</p>"}]}